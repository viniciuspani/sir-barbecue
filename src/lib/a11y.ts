// Constantes de acessibilidade (RNF-05) — uma das usuárias tem baixa visão.
// Aplicar em componentes de UI (alvos de toque, tamanhos mínimos de fonte).

/** Alvo mínimo de toque, em dp. */
export const MIN_TOUCH_TARGET = 48;

/** Tamanho mínimo de fonte do corpo, em sp. */
export const MIN_BODY_FONT = 16;

/** Tamanho mínimo de fonte para ações/botões, em sp. */
export const MIN_ACTION_FONT = 18;
